# Rides-Request-Demand-Forecast
The seeks to build and deploy a time series mode for predicting ride requests (demand) for a particular latitude and longitude for a requested future time window (duration)
